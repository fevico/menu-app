import { FC } from "react";
import { Text } from "react-native";

interface Props {}

const Profile: FC<Props> = (props) => {
    return (
        <Text>Profile</Text>
    )
};

export default Profile;