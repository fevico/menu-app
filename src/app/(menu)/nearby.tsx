import { FC } from "react";
import { Text } from "react-native";

interface Props {}

const NearBy: FC<Props> = (props) => {
    return (
        <Text>Nearby Items</Text>
    )
};

export default NearBy;