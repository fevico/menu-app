import { FC } from "react";
import { Text } from "react-native";

interface Props {}

const MenuCard: FC<Props> = (props) => {
    return (
        <Text>Menu Card</Text>
    )
};

export default MenuCard;