import { FC } from "react";
import { Text } from "react-native";

interface Props {}

const MenuBanner: FC<Props> = (props) => {
    return (
        <Text>Menu Banner</Text>
    )
};

export default MenuBanner;